spring:
  application:
    name: eureka-config-server  # 应用名称
  cloud:
    config:
      server:
        git:
          uri: git@github.com:linxianmin/cloud-config-test.git #配置文件所在仓库
          default-label: master #配置文件分支
          search-paths: config  #配置文件所在根目录